import { contactUI } from './contact';
import { addressUI, countryUI } from 'ui/common';

export default {
    contact: contactUI,
    address: addressUI,
    country: countryUI,
    province: countryUI,
    city: countryUI,
    county: countryUI,
}